{% extends 'base.html.twig' %}
{% block title %}Mes Factures{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header d-flex align-items-center ">
            <i class="fa fa-envelope-open-o success font-large-2 float-left"></i>
            <h3 class="mb-0 ml-1">Mes Factures</h3>
        </div>
        <div class="card-body">
            <link rel="stylesheet" href="{{ asset('css/home.css') }}">

            <div class="grey-bg container-fluid">
                <section id="minimal-statistics">
                    <div class="row">
                    </div>
                    <div class="row">
                        <div class="col-xl-3 col-sm-6 col-12">


                        </div>
                    </div>
                </section>
        </div>
            <a class="btn btn-success btn-lg px-3"
               href="{{ path('account_index') }}">Retour</a>
    </div>
{% endblock %}